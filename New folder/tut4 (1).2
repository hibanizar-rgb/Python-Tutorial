


class Rectangle:
    def __init__(self, length, width):
        self.length = length
        self.width = width

    # Method to calculate area
    def area(self):
        return self.length * self.width

# Example usage
# Create an object of Rectangle with given length and width
rect = Rectangle(10, 5)

# Display the area
print("Area of the rectangle:", rect.area())